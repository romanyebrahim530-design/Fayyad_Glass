name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Generate Android Files
        run: flutter create --project-name fayyad_glass --platforms=android .

      - name: Set up local.properties correctly
        run: |
          FLUTTER_SDK_PATH=$(dirname $(dirname $(which flutter)))
          echo "flutter.sdk=$FLUTTER_SDK_PATH" > local.properties
          echo "flutter.sdk=$FLUTTER_SDK_PATH" > android/local.properties

      - name: Restore Google Services
        run: git checkout android/app/google-services.json || true

      - name: Fix Min SDK to 21
        run: |
          sed -i 's/minSdkVersion.*/minSdkVersion 21/g' android/app/build.gradle || true

      - name: Get Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk
